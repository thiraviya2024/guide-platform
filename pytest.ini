[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
# Keep pytest's per-test tmp_path tree inside the writable checkout.  On some
# Windows profiles the default %TEMP%/pytest-of-<user> directory is ACL-locked.
addopts = --basetemp=.pytest_tmp
